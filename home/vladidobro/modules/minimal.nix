{ ... }:
{
  programs.ssh = {
    enable = true;
  };

  programs.bash = {
    enable = true;
  };

  programs.tmux = {
    enable = true;
  };

  programs.neovim = {
    enable = true;
  };

  programs.git = {
    enable = true;
  };
}
